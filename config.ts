export const APP_HOST = Deno.env("APP_HOST") || "127.0.0.1"
export const APP_PORT = Deno.env("APP_PORT") || 4000
export const DB_USER = Deno.env("DB_USER") || "meteo"
export const DB_DATABASE = Deno.env("DB_DATABASE") || "meteo"
export const DB_HOST = Deno.env("DB_HOST") || "localhost"
export const DB_PORT = Deno.env("DB_PORT") || "5445"
export const DB_PASSWORD = Deno.env("DB_PASSWORD") || "meteo"
